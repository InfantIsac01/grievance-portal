import './globals.css'
export const metadata = { title: 'HIT Grievance Portal', description: 'Student Grievance Management System — Hindusthan Institute of Technology' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
